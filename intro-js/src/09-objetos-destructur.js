

const user = {
    username:'andres',
    email:'correo@google.com',
    age:20,
    ranking:9,
}

const {username, ranking, age} = user;//desestructuracion

console.log(`${username} tiene  ${age} de edad`);
console.log(ranking);